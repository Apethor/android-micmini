# Normalize line endings: text files use LF in the repo (avoids CRLF churn on Windows).
* text=auto eol=lf

# Windows-only scripts keep CRLF
*.bat  text eol=crlf
*.cmd  text eol=crlf

# Binaries
*.apk  binary
*.jar  binary
*.png  binary
*.keystore binary
